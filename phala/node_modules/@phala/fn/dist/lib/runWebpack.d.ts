import { Configuration, Stats, StatsCompilation } from 'webpack';
export declare const MAX_BUILD_SIZE: number;
export declare function runWebpack({ buildEntries, projectDir, outputDir, outputFileName, customWebpack, isDev, clean, isAsync }: {
    buildEntries: Configuration['entry'];
    projectDir: string;
    outputDir?: string;
    outputFileName?: string;
    customWebpack?: string;
    isDev: boolean;
    clean: boolean;
    isAsync?: boolean;
}): Promise<Stats>;
export declare function printFileSizesAfterBuild(json: StatsCompilation, maxSize?: number): {
    folder: string;
    name: string;
    size: number;
    sizeLabel: string;
    outputPath: string;
}[];

  if (body.root === "img") {

        // call open ai to return and image for the website inspired by the body.description
        const openaiApiKey = "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";
        const imagePrompt = `Create an image for a meme coin website inspired by: ${body.description}`;

        try {
            const imageResponse = await fetch('https://api.openai.com/v1/images/generations', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'Authorization': `Bearer ${openaiApiKey}`
                },
                body: JSON.stringify({
                    prompt: imagePrompt,
                    n: 1,
                    size: "1060x1060"
                })
            });

            if (!imageResponse.ok) {
                throw new Error(`OpenAI API responded with status: ${imageResponse.status}`);
            }

            const imageData = await imageResponse.json();
            const imageUrl = imageData.data[0].url;

            // Add the image URL to the result
            result = imageUrl;
            console.log("Generated image URL:", imageUrl);
        } catch (error) {
            console.error('Error fetching image:', error);
            result.message = error instanceof Error ? error.message : 'An unknown error occurred';
        }
    }